1. cp https://conan.io/center/xxx?tab=recipe to xxx.py
2. rename sources to xxx and add xxx to conandata.yml
3. add following to generate method
# MacOS can support both architectures, but Conan doesn't support it yet
if self.settings.os == "Macos":
    tc.blocks["apple_system"].values["cmake_osx_architectures"] = "arm64;x86_64"
4. modify default options to it should be, not use command line to set options.

